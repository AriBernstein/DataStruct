using System;
using System.Collections.Generic;
using DataStruct;

namespace DataStruct_Examples
{
    static class FractionalCascading {
        public static void Demo() {
            // Utils u = new Utils();
            // MergeSortNodes msn = new MergeSortNodes();
            // CoordinateNodeListGenerator cnlg = new CoordinateNodeListGenerator();
            // CoordinateNode[] nodeList = cnlg.getCoordinateNodeList(20, 3);
            // u.printNodeArray(nodeList);
            // Console.WriteLine();
            // msn.sort(nodeList, 2);
            // u.printNodeArray(nodeList);
            FractionalCascadingLists fcl = new FractionalCascadingLists(5, 5);
            
        }
    }
}
